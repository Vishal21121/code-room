export const MessageTypeEnum = {
    TEXT: "text",
    MEDIA: "media"
}

export const SenderTypeEnum = {
    BOT: "bot",
    USER: "user"
}

export const AvailableMessageTypes = Object.values(MessageTypeEnum)
export const AvailableSenderTypes = Object.values(SenderTypeEnum)